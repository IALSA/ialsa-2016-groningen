`./data/meta/response-profiles-live/` Directory

Contains `.csv` files with pattern of possible responses that DataSchema variables produced in each study. Updated every time `Ellis Island` script is executed. 
