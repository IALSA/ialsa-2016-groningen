`./data/meta/h-rules/` Directory

Contains logical rules for encoding harmonized variables. Each `.csv` file contains the pattern of possible responses that DataSchema variables produced in each study. 
